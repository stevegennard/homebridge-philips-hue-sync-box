{
  "watch": ["src"],
  "ext": "ts",
  "ignore": [],
  "exec": "tsc && homebridge -U ./test/hbConfig -I -D > $(pwd)/test/hbConfig/homebridge.log",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}
